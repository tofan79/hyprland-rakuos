export BROWSER=zen-browser
export TERM=xterm-ghostty

# XDG / Session
export XDG_CURRENT_DESKTOP=Hyprland
export XDG_SESSION_DESKTOP=Hyprland
export XDG_SESSION_TYPE=wayland

# Toolkit / Qt / Cursor
export QT_QPA_PLATFORM="wayland;xcb"
export QT_QPA_PLATFORMTHEME=qt6ct
export QT_WAYLAND_DISABLE_WINDOWDECORATION=1
export QT_AUTO_SCREEN_SCALE_FACTOR=1
export XCURSOR_THEME=Bibata-Modern-Ice
export XCURSOR_SIZE=24
export HYPRCURSOR_THEME=Bibata-Modern-Ice
export HYPRCURSOR_SIZE=24
export GDK_BACKEND="wayland,x11"
export SDL_VIDEODRIVER="wayland,x11,windows"
export CLUTTER_BACKEND=wayland
export ELECTRON_OZONE_PLATFORM_HINT=auto
export GSK_RENDERER=gl

# GPU Hybrid — Default: AMD iGPU (hemat daya)
# Render default lewat AMD. NVIDIA hanya untuk app yg dijalankan dgn offload.
export LIBVA_DRIVER_NAME=radeonsi          # video decode di AMD iGPU (tetap aktif)
# export __GLX_VENDOR_LIBRARY_NAME=nvidia   # OFF (default AMD)
# export GBM_BACKEND=nvidia-drm             # OFF (default AMD)
# export __NV_PRIME_RENDER_OFFLOAD=1        # OFF (default AMD)
# export __VK_LAYER_NV_optimus=NVIDIA_only  # OFF (default AMD)
export SDL_VIDEO_MINIMIZE_ON_FOCUS_LOSS=0
